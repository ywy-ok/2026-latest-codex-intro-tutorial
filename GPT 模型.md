# 2026/9/10

## 可用模型

> 以下的模型代号灵感来自于拉丁文语中的天体，象征了不同的能力层级

GPT-5.5

GPT-5.6 系列：

1. Luna（月亮）

    适合**高频率、低复杂度的批量任务**，如文本分类、简单摘要、批量处理等。成本最低。

2. Terra（地球）

    适合**日常开发、内容创作、数据分析**等中等复杂任务。性能接近GPT-5.5，但成本仅为其一半。

3. Sol（太阳）

    GPT-5.6系列的**旗舰型号**。适合**高难度推理、复杂编程、科学研究**等专业工作。

GPT-6 系列：

1. Astra（星辰）

    特点是 **"多智能体"长时间协作**，能自主操作电脑、执行复杂软件工程任务。



## 订阅套餐

ChatGPT 当前的使用方式应区分三件事：模型、推理强度和产品入口。标准 Chat 中，Free/Go 主要使用 GPT-5.6 Luna；Plus 可使用 GPT-5.6 Sol，并通过 Instant、Medium、High、Extra High 等档位调节推理强度。Work 和 Codex 还可按需选择 Sol、Terra、Luna 及 Astra，并与标准 Chat 使用独立的额度体系。  

限额并非统一按 Token 或固定消息数计算：GPT-6 Astra 在标准 Chat 中有明确消息配额；Work/Codex 则可能同时受 5 小时和周额度约束，实际消耗取决于模型、任务复杂度、上下文长度、推理强度和 Fast 模式。应以应用内“设置 → 用量”显示的剩余额度与重置时间为准。

### Plus

**5小时限额**：一个**滚动计算**的短周期用量上限。并非让你连续使用5小时，而是限制在任意**5小时**窗口内能消耗的计算资源。官方**没有固定 Token 数**，实际消耗由任务复杂度决定。

**周限额**：一个周期更长的总量上限。**每7天**自动重置一次。也可通过付费购买“即时重置”立即恢复。同样**没有固定 Token 数**，实际消耗由任务复杂度决定。

